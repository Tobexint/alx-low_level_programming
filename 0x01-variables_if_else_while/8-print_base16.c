#include <stdio.h>

/**
 * main - Prints numbers from 0 to 15.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 16; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
